@model Pensive.Models.Credential
@{
	ViewData["Title"] = "Login";
}

<div class="row v-center">
	<div class="col-md-4 col-md-offset-4">
		<h3 class="text-center">@ViewData["Title"]</h3>

		<form method="post" novalidate>
			<div class="text-danger" asp-validation-summary="ModelOnly"></div>
			<div class="form-group">
				<label class="control-label" asp-for="UserName"></label>
				<input class="form-control" asp-for="UserName" />
				<span class="pull-right" asp-validation-for="UserName"></span>
			</div>
			<div class="form-group">
				<label class="control-label" asp-for="Password"></label>
				<input class="form-control" type="password" asp-for="Password" />
				<span class="pull-right" asp-validation-for="Password"></span>
			</div>
			<div class="form-group">
				<input type="submit" value="Login" class="btn btn-success" />
				<a class="pull-right" asp-action="Register">Create an Account</a>
			</div>
		</form>

	</div>
</div>